# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Clears the cart to prepare or finalize gateway operation tests.
mutation EmptyCart($userId: ID!) {
  emptyCart(userId: $userId) {
    userId
    items {
      productId
      quantity
      product {
        id
        name
      }
    }
  }
}
