# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Valide la récupération de publicités
query Ads($contextKeys: [String!]!) {
  ads(contextKeys: $contextKeys) {
    text
    redirectUrl
  }
}
