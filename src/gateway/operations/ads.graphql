# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Validates ad retrieval for the provided context keys.
query Ads($contextKeys: [String!]!) {
  ads(contextKeys: $contextKeys) {
    text
    redirectUrl
  }
}
