# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

# Valide le service currency-dgs
query Currency {
  supportedCurrencies
  convertCurrency(
    from: {
      currencyCode: "USD"
      units: 100
      nanos: 0
    }
    toCode: "EUR"
  ) {
    currencyCode
    units
    nanos
  }
}
