diff --git a/.env b/.env
index 764bc36..66d623b 100644
--- a/.env
+++ b/.env
@@ -146,9 +146,16 @@ GRAPHQL_ENABLE_INTROSPECTION=true
 # ******************
 # Currency DGS
 CURRENCY_DGS_PORT=5101
-CURRENCY_DGS_ADDR=currency-dgs:${CURRENCY_DGS_PORT}
+CURRENCY_DGS_ADDR=http://currency-dgs:${CURRENCY_DGS_PORT}/graphql
 CURRENCY_DGS_DOCKERFILE=./src/currency-dgs/Dockerfile
 
+# ******************
+# GraphQL Gateway
+# ******************
+GRAPHQL_GATEWAY_PORT=5000
+GRAPHQL_GATEWAY_ADDR=graphql-gateway:${GRAPHQL_GATEWAY_PORT}
+GRAPHQL_GATEWAY_DOCKERFILE=./src/gateway/Dockerfile
+
 # ******************
 # Dependent Services
 # ******************
diff --git a/docker-compose.yml b/docker-compose.yml
index 7a5c02f..4c62cb6 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -687,6 +687,39 @@ services:
       currency:
         condition: service_started
     logging: *logging
+  
+  # ******************
+  # GraphQL Gateway
+  # ******************
+    # ******************
+  # GraphQL Gateway
+  # ******************
+  graphql-gateway:
+    image: ${IMAGE_NAME}:${DEMO_VERSION}-gateway
+    container_name: graphql-gateway
+    build:
+      context: ./
+      dockerfile: ${GRAPHQL_GATEWAY_DOCKERFILE}
+      cache_from:
+        - ${IMAGE_NAME}:${IMAGE_VERSION}-gateway
+    deploy:
+      resources:
+        limits:
+          memory: 100M
+    restart: unless-stopped
+    ports:
+      - "${GRAPHQL_GATEWAY_PORT}"
+    environment:
+      - GRAPHQL_GATEWAY_PORT
+      - CURRENCY_DGS_ADDR
+      - OTEL_RESOURCE_ATTRIBUTES=${OTEL_RESOURCE_ATTRIBUTES},service.criticality=high
+      - OTEL_SERVICE_NAME=gateway
+      - GRAPHQL_ENABLE_UI
+      - GRAPHQL_ENABLE_INTROSPECTION
+    depends_on:
+      currency-dgs:
+        condition: service_started
+    logging: *logging
 
   # ******************
   # Dependent Services
